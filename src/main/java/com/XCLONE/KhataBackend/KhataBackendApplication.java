package com.XCLONE.KhataBackend;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class KhataBackendApplication {

	public static void main(String[] args) {
		SpringApplication.run(KhataBackendApplication.class, args);
	}
	/*
	Nothing much to say here, just the main application class that bootstraps the Spring Boot application. The @SpringBootApplication annotation is a convenience annotation that adds all of the following:
	 */
}
